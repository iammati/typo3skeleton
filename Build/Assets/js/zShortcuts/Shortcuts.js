/**
 * Shortcuts.
 * 
 * Define all shortcuts which can be
 * handy during development and easier to call.
 */

const Breakpoint = App.Utility.BreakpointUtility;

const EqualHeight = App.Utility.EqualHeightUtility;

const CEResolver = App.Resolver.CEResolver;

const CEirreResolver = App.Resolver.CEirreResolver;
