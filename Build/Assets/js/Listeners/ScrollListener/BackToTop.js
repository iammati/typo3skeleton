// Example for the ScrollListener - a simple BackToTop
// App.Listeners.ScrollListener.push(
//     scrollTop => {
//         const $backToTop = Elements.$backToTop;

//         if (scrollTop >= 200 && !$backToTop.hasClass('active')) {
//             $backToTop.addClass('active');
//         }

//         if (scrollTop < 200 && $backToTop.hasClass('active')) {
//             $backToTop.removeClass('active');
//         }
//     }
// );
